# Figma Desktop Bridge (Legacy)

Legacy bridge plugin kept for compatibility with upstream architecture.

## Status

For this repository MVP (`FigJam Console MCP`), the primary runtime bridge is:

- `figjam-desktop-bridge/`

Use `figma-desktop-bridge` only if you intentionally run Design-mode flows.

## Notes

- This folder is retained to minimize drift from upstream structure.
- Product focus and active roadmap are FigJam-first.
